"""History command to view past scans."""

# import typer


def history_command(
    limit: int = 10,
    output_format: str = "table",
) -> None:
    """
    Display history of past scans.

    Args:
        limit: Maximum number of scans to display
        output_format: Output format (table, json)
    """
    pass
