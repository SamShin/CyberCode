"""
CLI Scan command.
"""
# Imports

def scan(path_or_url: str, export: str = None, key_id: int = None):
    '''vulnlens scan <path_or_url> — submits scan, polls /scans/{id}/status with a progress indicator, prints full formatted report on completion via formatter_cli.py. Flags: --export json, --export html, --key-id <id> (select which saved key to use).'''
    pass
